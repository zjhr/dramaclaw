# GUO Skeleton Model Package

Optional mounted skeleton model package for GUO Canvas. Keep this directory beside frontend and backend under project root.

The application can start without this package; skeleton model listing should simply show an unavailable state when the package is missing.
